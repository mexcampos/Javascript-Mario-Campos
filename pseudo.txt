Muestra el formulario
    se muestra por defecto
Resultado oculto
    se oculta con la clase "oculto"
click en submit
    oculta el formulario => eventListener("submit", ocultar formulario)
    function ocultarForm () {
        add class "oculto" to form
    }
    muestra el resultado => eventListener("submit, mostrar)
    function mostrarResultado {
        remove class "oculto" from resultado
    }
    
let formulario = document.getElementById("formulario");
let cajaResultado = document.getElementById("resultado");

formulario.addEventListener("submit", function(event) {
  event.preventDefault();

  ocultarForm();
  mostrarResultado();
});

function ocultarForm() {
  formulario.classList.add("oculto");
}

function mostrarResultado() {
  resultado.classList.remove("oculto");
}


  // Check si todos los inputs estan seleccionados
  if (
    conocimientoInput &&
    experienciaInput &&
    ahorrosInput &&
    inversionInput &&
    tiempoInput &&
    objetivoInput &&
    actitudInput
  ) {
    // Agrega las respuestas al array
    respuestas.push(parseInt(conocimientoInput.value));
    respuestas.push(parseInt(experienciaInput.value));
    respuestas.push(parseInt(ahorrosInput.value));
    respuestas.push(parseInt(inversionInput.value));
    respuestas.push(parseInt(tiempoInput.value));
    respuestas.push(parseInt(objetivoInput.value));
    respuestas.push(parseInt(actitudInput.value));

    // Log de respuestas
    console.log(respuestas);
  } else {
    alert('Please select an option for all questions.');
  }